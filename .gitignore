# ─── Environment & secrets (never commit real keys) ──────────────────────────
.env
.env.*
!.env.example
!.env.sample
!.env.template
!.env.*.example
backend/.env
frontend/.env
*.secrets.env
*.local.secrets

# Tool-specific env loaders that often hold secrets
.envrc
.direnv/

# Key material & cloud credential files
*.pem
*.key
!*.pem.example
*-private-key.json
credentials.json
**/credentials.json
service-account*.json
**/service-account*.json
client_secret_*.json
thinktarteeb-*.json
*.keystore
*.jks

# Local / backup env copies (explicit; also covered by .env.* above)
*.env.backup
*.env.bak

rediscli.txt
commit.txt
Book1.csv
walkthrough*/

# DB sync dumps / local Supabase experiments (never commit)
*.dump
.cloud-sync-staging/
scratch/
tmp/
backend_addition/
scripts/sync-cloud-*.ps1

# TypeScript incremental build
*.tsbuildinfo

# Supabase CLI cache (migrations live under backend/supabase/)
backend/supabase/.temp/
supabase/

# Python
__pycache__/
*.pyc
*.pyo
venv/
env/
.venv/

# Node
node_modules/
dist/
build/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# AI / Agents / IDEs (not part of the app)
.agents/
.claude/
.gitnexus/
.cursor/
.windsurf/
.vscode/
.idea/
docs/gitnexus-*
envsec/

# Documents
*.md
!README.md
!readme.md
!Readme.md

# Local notes / exports / walkthroughs (do not commit)
*.pdf
*.csv
walkthrough*
*walkthrough*

skills-lock.json

envsec/